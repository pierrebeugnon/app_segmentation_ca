feat: popup de confirmation aussi pour les effectifs nouvellement créés

Lors de la suppression d'un effectif ajouté manuellement (bouton
"Ajouter un nouveau conseiller"), on vérifie maintenant s'il porte une
charge dans le tableau Répartition.

- Si charge détectée : popup `ConfirmDialog` demandant validation.
- Sur confirmation : suppression de l'effectif ET remise à null des ETP
cibles associés dans toutes les lignes du tableau Répartition.
- Extraction d'une méthode helper `ConfirmRemoveSlotWithChargeAsync`
pour partager la logique entre `RemoveNewSlot` et
`RemoveCibleForExistingSlot`.

Build OK (0 erreur).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
