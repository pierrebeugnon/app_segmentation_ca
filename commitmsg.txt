feat: vue Nombre de clients en consultation avec conversion automatique

Page PortefeuilleDimensionnement :

- En mode "Nombre de clients" (vue ETP désactivée) :
  - Toutes les actions d'édition du tableau Répartition sont désactivées :
    * bouton "Proposition de répartition" masqué
    * colonnes de saisie manuelle (transferts BP / PF mutualisé) masquées
    * cellules cibles affichées en lecture seule
  - Les valeurs ETP internes sont converties en nombre de clients à
    l'affichage via le service EtpConversionService (ETP × taille théorique).
  - Les totaux, agrégats par métier, colonne d'aide à la répartition et
    ligne "Part de nouveaux clients" utilisent des méthodes d'affichage
    converties en nombre de clients.

- Composant DimTableauEffectifs : ajout d'un paramètre ReadOnly. En mode
  consultation, les boutons d'ajout/retrait/restauration et les champs
  ETP cible sont masqués/remplacés par du texte.

- EtpConversionService : ajout de GetClientCountFromEtp(etp, segment).

Build OK (0 erreur).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
