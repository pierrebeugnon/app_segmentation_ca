refactor: restaure le chargement réel des données dans le code-behind

Après avoir aligné le .razor.cs sur la version client, les données étaient
passées sur les mocks. Ce commit réintroduit le chargement HTTP de
`api/SegmentationDistributive` tout en gardant la structure du .razor.cs
(client) et les méthodes du markup récent (charges manuelles, totaux KPI).

- Ajoute `[Inject] HttpClient` et `LoadDataAsync`.
- Ajoute `_segmentationDistributive` / `_segmentationDistributiveFiltree`.
- `BuildSlots` utilise les données réelles quand elles sont disponibles,
  avec fallback sur les mocks si l'API ne retourne rien.
- Ajoute la propriété `Etp` manquante dans le shim
  `SegmentationDistributiveData`.

Build OK (0 erreur).

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
